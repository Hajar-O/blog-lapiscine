{% extends 'admin/base.admin.html.twig' %}

{% block content %}

    <section>

        <table>

            <tr>
                <td>Id</td>
                <td>Titre</td>
                <td>dernière modification</td>
                <td>Modifier</td>
                <td>Supprimer</td>
            </tr>
            {% for categorie in categories %}

                <tr>
                    <td><p>{{ categorie.id }}</p>
                    <td><p>{{ categorie.title }}</p></td>
                    <td><p>{{ categorie.createdAt | date('d/m/y') }}</p></td>
                    <td><a href="{{ path('admin_category_update',{id:categorie.id}) }}">UP DATE</a></td>
                    <td><a href="{{ path('admin_category_delete',{id:categorie.id}) }}">DELETE</a></td>
                </tr>







            {% endfor %}
        </table>


{% endblock %}